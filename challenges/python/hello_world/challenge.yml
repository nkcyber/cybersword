# This file represents the base specification of your challenge. It is used by
# other tools to install and deploy your challenge.

# Required sections
name: "Hello, World!"
author: "Zack Sargent"
category: Python
description: |
    Start your programming journey with Python

    <a href="/ide/#challenge=0" target="_blank">
        <h2>
            Click Here!
        </h2>
    </a>
# prompt is inserted into the editor
prompt: |
    # Welcome to the Cyber IDE!
    # You are currently running Python 3.

    # A journey of a thousand miles begins with a single step.

    # This is a comment because it starts with '#'.
    # Lines that don't start with '#' are code and will be executed.

	# Delete/Backspace the '# ' from the last line to uncomment it,
	# and start your coding adventure.

    # print('hello, world')
value: 10
type: standard

# connection_info is used to provide a link, hostname, or instructions on how to connect to a challenge
# connection_info: /ide/#challenge=0

# Flags specify answers that your challenge use. You should generally provide at least one.
# Can be removed if unused
# Accepts strings or dictionaries of CTFd API data
flags:
    # A static case sensitive flag
    - cybersword{avast_ye_landlubbers}

# Topics are used to help tell what techniques/information a challenge involves
# They are generally only visible to admins
# Accepts strings
topics:
    - python

# Hints are used to give players a way to buy or have suggestions. They are not
# required but can be nice.
# Can be removed if unused
# Accepts dictionaries or strings
hints:
    - Code is for people! Not just computers. We can give the reader more information with comments, starting with "#".
    - Please let an instructor know if you have difficulties running the code.

# Requirements are used to make a challenge require another challenge to be
# solved before being available.
# Can be removed if unused
# Accepts challenge names as strings or challenge IDs as integers
requirements:
    - "Welcome"

# The state of the challenge.
# If the field is omitted, the challenge is visible by default.
# If provided, the field can take one of two values: hidden, visible.
state: visible

# Specifies what version of the challenge specification was used.
# Subject to change until ctfcli v1.0.0
version: "0.1"
